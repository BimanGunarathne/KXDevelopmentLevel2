// 1. Create a function that creates a global variable LAST_CALLED which stores the time that the function was last called at.
// This function should take two inputs, and return the result of multiplying them together.

f:{LAST_CALLED::.z.t*x*y}
f[2;3]
