// 1. In kdb+/q the order of execution of a code command is right to left, also referred to as 'left of right
/ ight to left, we avoid the need to consult precedence tables. If it didn't operate in this way the whole statement would need to be read and then reconstructed before passing to the compiler based upon the specified operator hierarchy (think of BOMDAS!).
